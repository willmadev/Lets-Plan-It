mutation CreateCourse($input: CreateCourseInput!) {
  createCourse(input: $input) {
    ... on Course {
      __typename
      id
      courseName
      tasks {
        id
        title
      }
    }
    ... on MutateCourseError {
      __typename
      field
      message
    }
  }
}
