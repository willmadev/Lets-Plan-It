mutation UpdateTask($input: UpdateTaskInput!){
  updateTask(input: $input) {
    ...on Task{
      __typename
      id
      title
      course {
        id
        courseName
        taskCount
      }
      description
      due
      completed
    }
    ...on MutateTaskError {
      __typename
      field
      message
    }
  }
}