mutation UpdateTask($input: UpdateTaskInput!){
  updateTask(input: $input) {
    ...on Task{
      __typename
      id
      title
      course
      description
      due
      completed
    }
    ...on MutateTaskError {
      __typename
      field
      message
    }
  }
}